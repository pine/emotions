rootProject.name = 'emotions'

include ':app'

for (File f : rootDir.listFiles()) {
    if (f.directory && f.name.startsWith('lib-') || f.name.startsWith('tool-')) {
        include(":${f.name}")
        project(":${f.name}").projectDir = f
    }
}

